# V24 모바일 테스트 체크리스트

## iPhone Safari
- 페이지가 화면 너비에 맞고 좌우로 밀리지 않는지
- 직접 선택 관광지 사진이 카드 폭을 넘지 않는지
- 날짜 입력 4개가 화면 안에서 보이는지
- 일정 카드의 사진·설명·편집 버튼이 겹치지 않는지
- Travel 목록이 한 화면 너비 안에서 보이는지
- 지역 필터만 가로로 스크롤되는지
- 상세보기가 화면 하단 전체 폭으로 열리는지
- `닫기 ×` 버튼이 항상 화면 상단에 보이는지
- 상세창 사진이 화면 폭에 맞는지

## Android / WeChat
- 화면 자동 축소 없이 정상 글자 크기로 표시되는지
- 페이지 전체에 가로 스크롤이 생기지 않는지
- 상세보기와 일정 등록 모달이 정상 스크롤되는지
- 닫기 버튼이 브라우저 상단 UI에 가려지지 않는지
